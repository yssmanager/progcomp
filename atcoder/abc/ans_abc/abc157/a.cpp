#include <bits/stdc++.h>
#define rep(i, a, n) for (int i = (a)); i < (n); ++i)
#define all(v) v.begin(), v.end()
using namespace std;
using ll = long long;
using P = pair<int,int>;

int main() {
    int n;
    cin >> n;
    int ans = (n+1)/2;
    cout << ans << endl;

    return 0;
}